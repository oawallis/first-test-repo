export type ReplayValidationEvidence = {\n  issueKey: string;\n  tenantId: string;\n  lastSuccessfulSync: string;\n  failureReason?: 'permission_denied' | 'rate_limited' | 'schema_mismatch';\n};\n\nexport function shouldRetryReplay(evidence: ReplayValidationEvidence): boolean {\n  return evidence.failureReason === 'rate_limited' || evidence.failureReason === 'schema_mismatch';\n}\n