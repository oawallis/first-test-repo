export type ReplayReceipt = {\n  issueKey: string;\n  syncRunId: string;\n  receivedAt: string;\n  signature: string;\n};\n\nexport function buildReplayReceipt(issueKey: string, syncRunId: string, secret: string): ReplayReceipt {\n  const receivedAt = new Date().toISOString();\n  const signature = Buffer.from(issueKey + ':' + syncRunId + ':' + secret).toString('base64url');\n  return { issueKey, syncRunId, receivedAt, signature };\n}\n